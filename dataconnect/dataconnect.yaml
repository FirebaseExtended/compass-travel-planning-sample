specVersion: "v1alpha"
serviceId: "dataconnect"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "dataconnect"
      cloudSql:
        instanceId: "dataconnect-test"
connectorDirs: ["./connector"]
